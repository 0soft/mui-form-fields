---
name: FormPhoneField
menu: Components
route: /components/FormPhoneField
---

import Form from '../FormTest';
import FormPhoneField from '../../src/components/FormPhoneField';
import { Playground, Props } from 'docz';

# FormPhoneField

## Example

<Playground>
  <Form
    render={() => {
      return (
        <FormPhoneField icon="extension" name="fieldName" label="Field Label" />
      );
    }}
  />
</Playground>

## Properties

<Props of={FormPhoneField} />
